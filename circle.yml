machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/.docker"
  override:
    - bin/circle-docker-provision.sh elasticsearch

test:
  override:
    - ~/.docker/docker-compose run -d web
